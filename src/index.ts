/** Stable Cordis plugin name (matches cordis.patch.yml). */
export const name = 'dsh-companion-studio'

/**
 * The public core does not serve character art. Browser-local uploads and
 * independently installed companion packs provide all visual assets.
 */
export function apply(): void {}
